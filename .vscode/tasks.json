{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run-Code",
            "type": "shell",
            "command": "make clean && make && make run && make clean",
            "group": "build"
        }
    ]
}